# Secret Key for Flask Application (generate a secure random key)
SECRET_KEY=your-secret-key-here

# Email Configuration (for contact form notifications)
SENDER_EMAIL=your-email@gmail.com
PASSWORD=your-app-password-or-smtp-password
RECIVER_EMAIL=receiver-email@example.com

# Database URI (SQLite database will be created in instance folder)
DB_URI=sqlite:///blog.db
